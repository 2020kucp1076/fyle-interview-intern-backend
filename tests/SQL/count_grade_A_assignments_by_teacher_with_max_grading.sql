-- Write query to find the number of grade A's given by the teacher who has graded the most assignments
select count(assignments.id) from assignments where state="GRADED" and grade="A" and teacher_id = (select teacher_id from assignments where state="GRADED" group by teacher_id order by count(teacher_id) desc limit 1)